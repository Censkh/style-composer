import {TouchableNativeFeedback} from "react-native";
import {poly}                    from "../PolyComponent";

export const PolyTouchableNativeFeedback = poly(TouchableNativeFeedback);

export default PolyTouchableNativeFeedback;
