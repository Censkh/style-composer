import {TouchableHighlight} from "react-native";
import {poly}               from "../PolyComponent";

export const PolyTouchableHighlight = poly(TouchableHighlight);

export default PolyTouchableHighlight;
